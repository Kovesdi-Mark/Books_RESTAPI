info:
  name: update_authors
  type: http
  seq: 3

http:
  method: PUT
  url: "{{a}}/authors/2"
  body:
    type: json
    data: |-
      {
        "name": "Imre"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
